{% extends 'base.html.twig' %}

{% block body %}
    <h1>Check-Out Duties</h1>

    <table class="standard-view table table-striped table-bordered" id="staff-duty-assignments-table">
        <thead>
            <tr>
                <th class="no-export">View</th>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Position</th>
                <th>Assignment</th>
                <th>Notes</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                {% if user.assignmentCheckOut is not null or user.assignmentCheckOutNotes is not null %}
                    <tr>
                        <td class="view-link"><a href="{{ path('app_user_show', { 'id': user.id }) }}">{{ global_view_link | raw }}</a></td>
                        <td>{{ user.consolidatedFirstName }}</td>
                        <td>{{ user.lastName }}</td>
                        <td>{{ user.position }}</td>
                        <td>{{ user.assignmentCheckOut }}</td>
                        <td>{{ user.assignmentCheckOutNotes }}</td>
                    </tr>
                {% endif %}
            {% endfor %}
        </tbody>
    </table>

{% endblock %}
